<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= product.name %></title>
</head>
<body>
	<h1>Edit Product</h1>
	<form action="/products/<%= product._id %>?_method=PUT" method="POST">
		<label for="name">Enter Name</label>
		<input type="text" name="name" id="name" placeholder="Product's Name" value="<%= product.name %>">
		<br>
		<label for="price">Enter Price</label>
		<input type="number" name="price" id="price" placeholder="Product's Price" value="<%= product.price %>">
		<br>
		<label for="category">Product's Category</label>
		<select name="category" id="category">
			<option value="fruit" <% if (product.category === "fruit") { %>
			selected
			<% } %>>Fruit</option>
			<option value="vegetable" <% if (product.category === "vegetable") { %>
			selected
			<% } %>>Vegetable</option>
			<option value="dairy" <% if (product.category === "dairy") { %>
			selected
			<% } %>>Dairy</option>
		</select>
		<br>
		<button>Submit</button>
	</form>
	<a href="/products/<%= product._id %>">Cancel</a>
</body>
</html>